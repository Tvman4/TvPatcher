<?xml version="1.0" encoding="utf-8"?>

<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Required for Android 11+ package visibility when checking
         whether the two target packages are installed. -->
    <queries>

        <package android:name="com.AnotherAxiom.GorillaTag" />
        <package android:name="com.TvMods.GorillaTag" />

    </queries>

    <application
        android:allowBackup="false"
        android:label="TvPatcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.TvPatcher">

        <activity
            android:name=".MainActivity"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

    </application>

</manifest>
